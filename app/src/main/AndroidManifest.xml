<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="prgdm.fatecrl.projetofinal" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastrarPalavra"
            android:label="@string/title_activity_cadastrar" >
        </activity>
        <activity
            android:name=".ListarDicionario"
            android:label="@string/title_activity_listar" >
        </activity>
        <activity
            android:name=".EditarDicionario"
            android:label="@string/title_activity_editar" >
        </activity>

    </application>

</manifest>
